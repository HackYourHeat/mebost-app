# ── Core ──────────────────────────────────────────────
Flask==3.0.3
Werkzeug>=3.0.0
python-dotenv==1.0.1
requests==2.32.3
gunicorn==22.0.0

# ── Static files (Railway production) ─────────────────
whitenoise==6.7.0

# ── PostgreSQL (Railway Postgres plugin) ──────────────
# Bỏ comment dòng dưới KHI và CHỈ KHI bạn add PostgreSQL plugin trong Railway:
# psycopg2-binary==2.9.9
